import 'package:flutter/material.dart';

class VisitorsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Visitors'),
        backgroundColor: Colors.deepPurple,
      ),
      body: Center(
        child: Text('Visitors Page'),
      ),
    );
  }
}
